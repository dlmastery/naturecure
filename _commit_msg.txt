Mock-faithful redesign: DossierShell + tinnitus + dead-end fixes

The big one. Reproduces the founder's two mock systems (otc1 React app
+ NatureNicheCure1 deck) into every journey page. Founder mandate:
"faithful and clear reproduction of mocks to website with proper fidelity".

Mock examination & spec
- docs/MOCK_UX_REPORT.md (deck inventory + voice catalogue)
- docs/JOURNEY_REDESIGN_SPEC.md (11 sections, 35 components, 20 tests,
  6 phases, prescriptive build-against-verbatim)

Design tokens (globals.css)
- New @theme block from spec §1.5: paper-deeper, forest-tint, terracotta
  (+deep +soft), warn/-soft, ok/-soft, grade-a..x, phase-1..3 tints,
  radius-sm/md, shadow-lg, ease-spring, dur-fast/base/slow. Existing
  forest/gold/pastels preserved.
- Utility classes: eyebrow--accent, eyebrow--gold, callout (+ note/warn/ok
  variants), card-tinted, card-warn, rule-line.

Persistent nav (the #1 founder ask)
- components/layout/dossier-shell.tsx (master shell)
- components/nav/left-rail-nav.tsx (280px sticky rail, 4 groups, dot+label)
- components/nav/mobile-tab-strip.tsx (lg- horizontal scroll-snap chips)
- components/nav/mobile-dock.tsx (fixed [<] NN/NN [>] dock)
- components/nav/top-bar.tsx (Section NN/NN + chips + Build CTA)
- components/nav/step-counter.tsx (mono pill with data-component hook)
- lib/use-active-section.ts (IntersectionObserver scroll-spy + hash sync)
- lib/use-keyboard-nav.ts (←/→/J/K/1-9/Home/End ignoring inputs)
- AppShell suppresses its chrome on dossier routes (no double rail)

Section primitives
- components/layout/section-anchor.tsx (ordinal + ochre eyebrow +
  italic-forest serif title + lede)
- components/layout/section-footer.tsx (hairline + SC.NN cue)
- components/layout/manifesto-band.tsx (forest band, keyword + italic body)
- components/layout/foundation-pillar-row.tsx (the 01/02/03/04 row;
  Evidence first / Natural protocols / Recurring care / Honest boundaries)
- components/callouts/callout.tsx (warn/note/ok tones; Triangle/Info/Check)

Dossier integration
- lib/research.ts exports splitDossierByH2(body) -> [{id, title, body}]
- app/skin/vitiligo/page.tsx full refactor into <DossierShell> with 15
  section anchors. Hero is now Section 01 (Overview); foundation pillars
  rendered; dossier prose split by H2 and routed into tab buckets.
- app/[domain]/[slug]/page.tsx full refactor into <DossierShell>. Generic
  heuristic maps any dossier H2 to one of 15 canonical tabs (overview,
  science, phyto, traditions, biophysical, diet, lifestyle, combos, daily,
  interactions, scoring, timeline, suppliers, closing, references).

Tinnitus journey (51st)
- Added tinnitus-support to lib/data.ts under BrainEye domain at
  /ears/tinnitus. Atlas card surfaces it; the unified [domain]/[slug]
  picks it up; DossierShell renders identically to vitiligo.
- research/tinnitus-support.md (9,260 words, 3/3 internal ralph passes).
  Dr Oz handled honestly as a cultural reference with Lipoflavonoid
  explicitly NOT endorsed. Pulsatile tinnitus pulled out as a red-flag
  exception. Tebonin Ginkgo + Mg + measure-first Zn + NAC + Vinpocetine
  (TGA) + Pycnogenol + B12 + CoQ10 + L-arg/cit + low-dose melatonin
  + Lion's Mane + Bhramari pranayama as THE tinnitus practice.

Package flow rebuild (12 dead-ends fixed)
- New /checkout/confirm route (ConfirmCard + Activate Companion CTA)
- /companion reads ?onboarded=true&bundle=... and surfaces OnboardedBanner
- /expert reads ?from=safety&meds/preg/organ/allergies and surfaces the
  routed-from-safety banner with explicit flag context
- /c/[category]/package rewritten as a 3-state wizard (A=quiz, B=flagged
  expert routing, C=cleared + tier-driven 90-day pack)
- New components: SafetyQuiz (4 steps + progress bar + flag-list context),
  StepIndicator, NinetyDayPackCard, QualityStrip, ConfirmCard,
  OnboardedBanner. Tier pills now functional (Starter=one-time,
  Core=sub, Premium=sub + $89 expert chart upsell).

Vitiligo de-flagshipped
- Removed from the main AppShell rail's primary nav (was "Vitiligo |
  FLAGSHIP"). Vitiligo remains a first-class journey accessible at
  /skin/vitiligo via the atlas like any other ailment.

Playwright mock-fidelity suite (tests/mock-fidelity.spec.ts)
- 16 tests in 5 describe blocks: DossierShell chrome, editorial fidelity,
  tinnitus first-class, atlas count = 51 + tinnitus listed, package flow
  end-to-end (safety quiz cleared → pack; meds=Yes → expert routing;
  /checkout/confirm → /companion?onboarded=true).
- 12/16 currently passing; 4 selector-refinement followups noted.

Verification
- npx tsc --noEmit -p .  : exit 0
- npm run build           : exit 0  (75 routes prerendered including the
                                       new /checkout/confirm + tinnitus)
- Visual confirmation via chrome-devtools MCP screenshots of vitiligo,
  tinnitus, and /c/skin-pigmentation/package — all faithful to the mocks.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
