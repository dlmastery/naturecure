Wire 50 research dossiers into the journey pages

- research/ moved into nichecore/ so Vercel build can read it
- lib/research.ts reads + parses YAML frontmatter via gray-matter
- components/dossier.tsx renders the markdown body with on-brand
  editorial prose styles (Fraunces serif H1-H4 with hairline rules,
  forest links, gold list markers, mono table headers)
- Date coercion in DossierRender so YAML date values don't crash
  React (gray-matter parses ISO dates to JS Date objects)
- /[domain]/[slug] and /skin/vitiligo now embed their dossier
  with a metadata pill bar showing domain, last-updated, clinical
  scoring instrument, internal-ralph pass count, word count
- app/globals.css adds .dossier-prose typography rules

Production build: 73 routes prerendered, exit 0
Sample render check: /weight/belly-fat carries the full 8,564-word
dossier with 19 tables and 26 h3 sections; vitiligo carries 7,352
words; both visually verified through chrome-devtools MCP.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
